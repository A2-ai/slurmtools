% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wait-job.R
\name{wait_for_slurm_job}
\alias{wait_for_slurm_job}
\title{Wait for a job to finish}
\usage{
wait_for_slurm_job(job, poll = 10, timeout = Inf)
}
\arguments{
\item{job}{the result of \code{\link[=submit_slurm_job]{submit_slurm_job()}}, or one job id}

\item{poll}{seconds between two looks at \code{sacct}}

\item{timeout}{seconds to wait in total before giving up with an error;
\code{Inf} (the default) waits as long as it takes}
}
\value{
the job's final \code{\link[=slurm_job_status]{slurm_job_status()}} row, invisibly
}
\description{
Looks at \code{\link[=slurm_job_status]{slurm_job_status()}} every \code{poll} seconds until the job reaches a
terminal state — \code{COMPLETED}, \code{FAILED}, \code{CANCELLED}, \code{TIMEOUT},
\code{OUT_OF_MEMORY}, \code{NODE_FAIL}, \code{PREEMPTED}, \code{BOOT_FAIL} or \code{DEADLINE} — and
hands back that final status row. A job that failed is returned, not
thrown: read its \code{state} and \code{exit_code}. This is the R-side "post hook":
\code{wait_for_slurm_job(job)}, then ordinary R.

A job \code{sacct} does not know yet shows as "not in sacct yet" and is polled
like any other, so a fresh submission is never mistaken for a missing job.
}
\examples{
\dontrun{
job <- submit_slurm_job(rscript, slurm_template_opts = list(file = "sim.R"))
status <- wait_for_slurm_job(job)
status$state # "COMPLETED"
slurm_job_log(job)
}
}
