% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/job.R
\name{slurm_job_status}
\alias{slurm_job_status}
\title{Status of submitted jobs}
\usage{
slurm_job_status(job)
}
\arguments{
\item{job}{the \link{Job} returned by \code{\link[=submit_slurm_job]{submit_slurm_job()}}, or one or more job
ids}
}
\value{
a tibble with one row per job \code{sacct} knows: \code{job_id}, \code{job_name},
\code{partition}, \code{state} (\code{PENDING}, \code{RUNNING}, \code{COMPLETED}, \code{FAILED},
\code{CANCELLED}, ...), \code{exit_code} (\code{"0:0"} is success), \code{elapsed}, \code{node},
and \code{submit}, \code{start}, \code{end} as date-times (\code{NA} until they happen). A
job \code{sacct} does not know is simply absent.
}
\description{
Asks \code{sacct} about one job or several and returns one row per job.
\code{sacct} still knows a job after it has finished, where \code{squeue} (behind
\code{\link[=get_slurm_jobs]{get_slurm_jobs()}}) forgets it a few minutes later, so this is the call
for "did it work?" as well as "is it running?".
}
\examples{
\dontrun{
job <- submit_slurm_job(rscript, file = "sim.R", partition = "cpu2mem4gb")
slurm_job_status(job)
slurm_job_status(c(2051, 2052))
}
}
