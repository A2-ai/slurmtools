% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/template.R
\name{slurm_template_opts}
\alias{slurm_template_opts}
\title{What goes in \code{slurm_template_opts}}
\usage{
slurm_template_opts(template)
}
\arguments{
\item{template}{a \code{\link[=Template]{Template()}}}
}
\value{
a tibble with columns \code{option} (the placeholder), \code{used_by} (the
\code{--flag} it fills, or \code{script} for a command or hook line), \code{required},
\code{value} (the fill so far, \code{NA} if none) and \code{default} (what an unfilled
option becomes at submit, \code{NA} if it must be given)
}
\description{
One row per placeholder of the template — everything
\code{submit_slurm_job(template, slurm_template_opts = list(...))} or \code{\link[=fill]{fill()}}
can take — with what it feeds, whether the job can be submitted without
it, its value so far, and what it falls back to when left out.
}
\examples{
default_template("Rscript", "{{file}}") |>
  slurm_template_opts()
}
