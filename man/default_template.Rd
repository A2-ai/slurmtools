% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/default-template.R
\name{default_template}
\alias{default_template}
\title{A ready-made template to start from}
\usage{
default_template(
  command = NULL,
  args = character(),
  conditional = NULL,
  if_true = character(),
  if_false = character()
)
}
\arguments{
\item{command}{the program to run, e.g. \code{"Rscript"}, \code{"python3"} or
\code{"bbi"}; \code{NULL} (the default) for the header alone, to give a command
later with \code{\link[=with_command]{with_command()}}}

\item{args}{character vector of arguments appended to the command, one
word each, e.g. \code{c("nonmem", "run", "local", "{{file}}")}. An argument
containing a space is quoted for the shell}

\item{conditional}{a placeholder that decides between \code{if_true} and
\code{if_false}, e.g. \code{"parallel"}; \code{NULL} (the default) for a plain line}

\item{if_true, if_false}{character vectors of arguments appended when
\code{conditional} is true, respectively false, e.g.
\code{if_true = c("--parallel", "--threads={{ncpu}}")} and
\code{if_false = character()}}
}
\value{
a \code{\link[=Template]{Template()}}
}
\description{
The header most jobs need, in the shape of the NONMEM template slurmtools
has always shipped — one node, one task, \code{ncpu} cpus on it, a partition,
and an account when you give one — plus the command when you pass one:
\code{default_template("Rscript", "{{file}}")} is a complete job. Add anything
more with the \verb{with_*()} verbs; \code{\link[=with_sbatch_flag]{with_sbatch_flag()}} takes any sbatch flag,
\code{\link[=with_pre_run]{with_pre_run()}} and \code{\link[=with_post_run]{with_post_run()}} add lines around the command.\tabular{lll}{
   placeholder \tab fills \tab if you leave it out \cr
   \code{job_name} \tab \code{--job-name} \tab the stem of \code{file} (\code{sim.R} → \code{sim}) \cr
   \code{ncpu} \tab \code{--cpus-per-task} \tab \code{1} \cr
   \code{partition} \tab \code{--partition} \tab the first partition \code{get_slurm_partitions()} lists \cr
   \code{account} \tab \code{--account} \tab the line is dropped \cr
}


\code{--nodes=1} and \code{--ntasks=1} are filled already; fill \code{nodes} or \code{ntasks}
again to change them. Logs go to \verb{<submission_root>/<job_name>-\%j.out} and
\code{.err} unless you add \code{\link[=with_output]{with_output()}} / \code{\link[=with_error]{with_error()}}.
\code{\link[=slurm_template_opts]{slurm_template_opts()}} lists the placeholders of the template you end up
with.
}
\examples{
default_template()

rscript <- default_template("Rscript", "{{file}}") |>
  with_sbatch_flag("time")
\dontrun{
# job "sim", 1 cpu, first partition
submit_slurm_job(rscript, slurm_template_opts = list(file = "sim.R", time = "01:00:00"))
submit_slurm_job(rscript, partition = "cpu4mem32gb", ncpu = 4,
                 slurm_template_opts = list(file = "sim.R", time = "01:00:00"))
}
}
