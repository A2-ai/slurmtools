% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/template.R
\name{write_slurm_template}
\alias{write_slurm_template}
\title{Write a template to a file}
\usage{
write_slurm_template(template, path, overwrite = FALSE)
}
\arguments{
\item{template}{a \code{\link[=Template]{Template()}}}

\item{path}{where to write the file, e.g. \code{"rscript.tmpl"}}

\item{overwrite}{whether to replace an existing file at \code{path}}
}
\value{
\code{path}, invisibly
}
\description{
Saves the template as it renders now — filled values in place, unfilled
placeholders as \code{{{name}}} — to a plain bash file you own. Open it, edit it,
add whatever your workflow needs; slurmtools does not look inside it again
except to fill the placeholders.
}
\examples{
\dontrun{
Template() |>
  with_sbatch("job-name") |>
  with_sbatch("partition") |>
  write_slurm_template("rscript.tmpl")
}
}
