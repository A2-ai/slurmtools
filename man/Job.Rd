% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/job.R
\name{Job}
\alias{Job}
\title{A submitted slurm job}
\usage{
Job(
  job_id = character(0),
  job_name = character(0),
  partition = character(0),
  submit_time = Sys.time(),
  script = character(0),
  output = character(0),
  error = character(0),
  sbatch = list()
)
}
\arguments{
\item{job_id}{the id sbatch returned, as a string}

\item{job_name}{the job's \code{--job-name}}

\item{partition}{the partition requested, or \code{NULL} for the cluster default}

\item{submit_time}{when sbatch accepted the job}

\item{script}{path of the rendered job script}

\item{output, error}{paths of the job's stdout and stderr logs, \verb{\%j}
resolved; \code{NA} when the template routes them elsewhere without a value}

\item{sbatch}{the \code{\link[processx:run]{processx::run()}} result of the sbatch call}
}
\description{
What \code{\link[=submit_slurm_job]{submit_slurm_job()}} returns for a \code{\link[=Template]{Template()}}: the job id sbatch
handed back, plus where the job's script and logs are, with \verb{\%j} already
resolved to the id. Print it to see them; the raw \code{sbatch} result is kept
under \verb{@sbatch}. Hand it to \code{\link[=slurm_job_status]{slurm_job_status()}} or \code{\link[=cancel_slurm_job]{cancel_slurm_job()}}.
}
